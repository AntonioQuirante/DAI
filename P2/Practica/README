Para ejecutar desde 0 esta práctica:
docker compose -f docker-compose.yml build
docker compose -f docker-compose.yml up (levanta mongo y el servidor en local en el puerto)

Para bajarlo
docker compose -f docker-compose.yml down --remove-orphans